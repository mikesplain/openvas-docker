machine:
  services:
    - docker

test:
  override:
    - mkdir logs
    - docker build -t openvas . 2>&1 | tee logs/build.log:
        timeout: 1200
    - sh test.sh 2>&1 | tee logs/test.log:
        timeout: 600

general:
  artifacts:
    - "logs"
